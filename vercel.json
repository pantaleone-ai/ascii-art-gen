{
  "version": 2,
  "functions": {
    "api/*.py": {
      "runtime": "vercel-python@0.5.2"
    }
  },
  "builds": [
    { "src": "next.config.mjs", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.py" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
